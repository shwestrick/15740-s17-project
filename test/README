This directory contains sample programs which are phase-concurrent, modulo C
library implementations (which might be implemented in a non phase-concurrent
manner). Due to the lack of a phase-concurrent scheduler, all programs are
either statically scheduled or scheduled via a custom scheduler.
